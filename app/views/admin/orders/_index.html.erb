<h4>注文履歴一覧</h4>

<table>
  <thead>
    <th>購入日時</th>
    <th>購入者</th>
    <th>注文個数</th>
    <th>注文ステータス</th>
  </thead>

  <tbody>
    <% orders.each do |order| %>
      <tr>
        <%# タイムゾーンの変更はまだしてないです。この記述だとコードが長いのでlメソッドの方法も検討%>
        <td><%= link_to order.created_at.strftime("%Y/%m/%d %H:%M:%S"), admin_order_path(order) %></td>
        <%# フルネーム表示するにはモデルでfull_nameメソッドを作り、そのままビューに記述する %>
        <td><%= order.members.full_name %></td>
        <%# 商品数とそれぞれの商品の個数を合わせないといけない？　ので合計値が欲しい。中間になってる注文商品から %>
        <td><%= order.item_orders.sum(:count) %></td>
        <%# 注文ステータスを日本語化するための記述 %>
        <td><%= order.status_i18n %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= paginate orders %>